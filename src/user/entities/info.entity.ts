export interface Birth {
    year: Number
    month: Number
    day: Number
}

export interface Profile {
    height: Number
    weight: Number
    job?: string
    company?: string
    address?: string
}